@using Politecnico.Patrones.ProyectoFinal.Contratos
@model Politecnico.Patrones.ProyectoFinal.Lib.MV.MVInterpreteDetallado

@{
    ViewBag.Title = "Detalle de Intérprete";
}

<h2>Detalle de Intérprete</h2>

<fieldset>
    <legend></legend>
    <table>
        <tr>
            <td>@Html.DisplayNameFor(model => model.Nombre)</td>
            <td>@Html.DisplayFor(model => model.Nombre)</td>
        </tr>
        <tr>
            <td>Álbumes</td>
            <td>@Html.Partial("~/Views/Albumes/_Albumes.cshtml", Model.Albumes)</td>
        </tr>
        <tr>
            <td>Canciones sueltas</td>
            <td>@Html.Partial("~/Views/Canciones/_Canciones.cshtml", Model.CancionesSinAlbum)</td>
        </tr>
    </table>
</fieldset>
<p>
    @Html.ActionLink("Editar", "Editar", new {id = Model.Id}) |
    @Html.ActionLink("Volver al listado", "Index")
</p>