@model IEnumerable<Politecnico.Patrones.ProyectoFinal.Lib.Entidades.Interprete>

@{
    ViewBag.Title = "Lista de intérpretes";
}

<h2>Listado de Intérpretes</h2>

<p>
    @Html.ActionLink("Crear intérprete", "Crear")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nombre)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.ActionLink("Editar", "Editar", new {id = item.Id}) |
                @Html.ActionLink("Ver detalle", "Detalle", new {id = item.Id}) |
            </td>
        </tr>
    }

</table>